export enum DomainStatus {
  ACTIVE = 'ACTIVE',
  INACTIVE = 'INACTIVE',
  SUSPENDED = 'SUSPENDED',
  SANDBOX = 'SANDBOX',
}

export enum DomainTransports {
  GRPC = 'GRPC',
  HTTPS = 'HTTPS',
  GRPC_HTTPS = 'GRPC_HTTPS',
}
